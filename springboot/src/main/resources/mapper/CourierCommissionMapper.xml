<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CourierCommissionMapper">

    <select id="selectAll" resultType="com.example.entity.CourierCommission">
        select * from `courier_commission`
        <where>
            <if test="name != null"> and name like concat('%', #{name}, '%')</if>
        </where>
        order by id desc
    </select>

    <delete id="deleteById">
        delete from `courier_commission`
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.CourierCommission" useGeneratedKeys="true">
        insert into courier_commission
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="commission != null">commission,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="commission != null">#{commission},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.CourierCommission">
        update courier_commission
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="commission != null">
                commission = #{commission},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>